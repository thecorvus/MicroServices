eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8775
spring:
  application:
    name: Ribbon-Hystrix-dashboard
logging:
  file: ${spring.application.name}.log